meta {
  name: Validate
}

docs {
  Tests for POST /api/v1/deployments:validate — the non-mutating J3
  tunable-allowlist dry-run used by the portal create wizard. Dev-open
  (no OIDC); always 200 with the verdict {mode, violations, blocked}.
}
